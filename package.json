{
  "name": "applylog",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "shared",
    "extension"
  ],
  "scripts": {
    "dev": "npm run dev -w @applylog/extension",
    "build": "npm run build -w @applylog/extension"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
